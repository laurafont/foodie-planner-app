{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd client && npm install && NODE_OPTIONS=--openssl-legacy-provider npm run build"
  },
  "deploy": {
    "startCommand": "node ./bin/www"
  }
}

